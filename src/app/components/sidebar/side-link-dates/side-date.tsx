import { Dates } from "@/app/interfaces/interfaces";

export default function SideDate(props: Dates) {
  return (
    <section>
      <h3>{props.title}</h3>
      <ul>
        <li></li>

      </ul>
    </section>
  );
}